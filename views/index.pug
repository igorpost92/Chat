doctype html
html(lang="en")
  head
    title= Title
    script.
      var gon = !{JSON.stringify(gon)};
  body
    #chat.d-flex.flex-column.flex-grow-1
    // block content
  script(src='/assets/main.js')
